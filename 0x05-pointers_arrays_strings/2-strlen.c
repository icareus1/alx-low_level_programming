#include "main.h"
/**
 * _strlen - count length of a string
 * @s: string value
 * Return: return the length of the string
 */
int _strlen(char *s)
{
	int length = 0;

	while (*s != '\0')
	{
		lentgh++;
		*s++;
	}
	return (length);
}
