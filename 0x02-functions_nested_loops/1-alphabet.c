#include "main.h"
/**
 * main - check the code
 * Return: Always 0.
 */
void print_alphabet(void)
{
    for (int ch = 'a'; ch <= 'z'; i++)
    {
	 _putchar(ch);
    }
    _putchar('\n');
    return (0);
}
