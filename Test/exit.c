#include "shell.h"
/**
 * sh_exit - Exit the shell.
 * @args: Null-terminated array of argument.
 * Return: 0 if successful, to signal that the shell should exit.
 */
int sh_exit(char **args)
{
	(void)args;
	return (0);
}
